#include <stdio.h> // Preprocessor or header files

int main(void) // main() is a function that takes no parameters and returns an integet
{
    printf("From sea to shining C.\n"); // printf() is a special function defined in standard library. \n - newline
    return 0;                           // returned 0 to OS, keeps the compiler happy
}